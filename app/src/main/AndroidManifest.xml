<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohamed.lostchilds">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Comments.Comments"></activity>
        <activity android:name=".View.Map.AddFoundMap" />
        <activity android:name=".View.Profile" />
        <activity android:name=".View.Map.MapsActivity" />
        <activity android:name=".View.SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Splash" />
        <activity android:name=".View.SignUp" />
        <activity android:name=".View.Main" />
        <activity android:name=".View.found.AddItem_Found" />
        <activity android:name=".View.Lost.AddItem_Lost" />
        <activity android:name=".View.news.AddItem_News" />
    </application>

</manifest>